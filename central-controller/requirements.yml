---
# Ansible Galaxy requirements for Central Controller playbooks
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: ansible.controller
    version: ">=4.0.0"
    source: https://console.redhat.com/
